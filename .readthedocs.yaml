version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  commands:
    - cd docs
    - sphinx-build -b html . $READTHEDOCS_OUTPUT/html

python:
  install:
    - requirements: docs/requirements.txt